// IMPORTANT: make sure the 'MAX_WIDTH_SMALL_SCREEN' value here matches with 'max-width' value in styles/base/_mixins.scss (@mixin style-for(...)).
export const MAX_WIDTH_SMALL_SCREEN = 1023;

// The speed which the email-loading overlay will fade out.
export const EMAIL_LOADING_OVERLAY_FADE_SPEED = 1000;